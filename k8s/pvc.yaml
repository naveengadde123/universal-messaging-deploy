apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: um-data-pvc
  namespace: um-deployment
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: standard
  resources:
    requests:
      storage: 10Gi
