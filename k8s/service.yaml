apiVersion: v1
kind: Service
metadata:
  name: deployment-1-service
  namespace: um-deployment
  labels:
    app: deployment-1
spec:
  selector:
    app: deployment-1
  type: LoadBalancer
  externalTrafficPolicy: Local
  ports:
    - name: nsp
      protocol: TCP
      port: 9900
      targetPort: 9900
